name: Build & Publish

on:
  push:
    branches:
      - master
    tags:
      - "*.*.*"

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.x

      - name: Build
        run: |
          dotnet build -c Release Source/YoloV8/YoloV8.csproj -p:Version=${{ github.ref_name }}
          dotnet build -c Release Source/YoloV8.Tests/YoloV8.Tests.csproj -p:Version=${{ github.ref_name }}

      - name: Test
        run: dotnet test Source/YoloV8.Tests/YoloV8.Tests.csproj

      - name: Pack
        run: |
          dotnet pack -c Release -o . Source/YoloV8/YoloV8.csproj -p:Version=${{ github.ref_name }}
          dotnet pack -c GpuRelease -o . Source/YoloV8/YoloV8.csproj -p:Version=${{ github.ref_name }}

      - name: Publish
        run: dotnet nuget push *.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicat

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >
          gh release create ${{ github.ref_name }}
          --repo ${{ github.event.repository.full_name }}
          --title ${{ github.ref_name }}
          --generate-notes
          --verify-tag
